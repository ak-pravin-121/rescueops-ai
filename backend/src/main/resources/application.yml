server:
  port: ${PORT:8080}
  forward-headers-strategy: framework

spring:
  application:
    name: rescueops-ai-backend

  datasource:
    url: url: jdbc:postgresql://dpg-d929aktckfvc-a.oregon-postgres.render.com:5432/rescueops?sslmode=require
    username: rescueops
    password: 5Ga5di5EWQRPvcB1Be6PQT8qNqPb2jEE
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

  jackson:
    default-property-inclusion: non_null

app:
  jwt:
    secret: ${JWT_SECRET:RescueOpsSecretKey2026SuperSecureJWTToken}
    expiration-ms: 604800000
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://rescueops-ai.onrender.com}

gemini:
  api-key: ${GEMINI_API_KEY:}
  model: gemini-2.5-flash
  embedding-model: gemini-embedding-001
  api-base-url: https://generativelanguage.googleapis.com/v1beta/models

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: INFO
    com.rescueops: DEBUG
